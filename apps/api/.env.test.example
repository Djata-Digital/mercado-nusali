# Banco EXCLUSIVO para testes de integração.
# O nome precisa conter "_test" para que as proteções da suíte permitam uso.
DATABASE_URL_TEST="postgresql://postgres:postgres@localhost:5432/mercado_nusali_test?schema=public"

# Redis real é opcional. Sem esta variável, workers BullMQ ficam desativados
# nas suítes que não precisam testar Redis de verdade.
# REDIS_URL_TEST="redis://localhost:6379/15"

# Chave AES-256-GCM de TESTE: Base64 que decodifica para exatamente 32 bytes.
LOGISTICS_ENCRYPTION_KEY="MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY="
